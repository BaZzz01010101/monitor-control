[package]
name = "dell-controller-tray"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
anyhow.workspace = true
dell-controller-core = { path = "../core" }
native-windows-gui = { version = "1.0", features = ["frame", "menu", "message-window", "tray-notification"] }

[[bin]]
name = "dell-controller-tray"
path = "src/main.rs"
